4小时
1创建用户与tenets
2创建资源:主机，镜像，密钥
3创建网络:公网与私网
4创建实例
5创建glance卷
6添加卷到主机
7创建卷快照
8自定义镜像，创建实例(安装httpd,这首页文件，创用户，不配密码，实例的密钥和安全组)
9创建heat stack(materials下提供了一个yaml模板，只读，需要复制出来，内容格式不对flavor顶格了，应该有两个空格，跟其他对齐，key_nam少了一个e，所有参数自己添加default值名称也不对，脚本需要添加一个unix组heatgroup)
10添加第二台主机(主机文件会提供，部署是使用脚本，需要修改脚本中的计算节点数据为2，添加要第二台主机后rc文件密码会重置为随机，需要修改会刚开始的密码)
11创建第二个实例，运行在compute1(ftpsever)
12将之前创建的实例迁移到compute1
14消息队列，发送消息
15ssh密钥访问
16将metric资源导出>另存文件
